class Complex {

    double real;
    double image;

    // write methods here
    public add(double num) {
        this.real = this.real + num.i;
        this.image = i + num.image;
    }
    public subtract(double num) {
        this.real = this.real - num.i;
        this.image = i + num.image;
    }
}
